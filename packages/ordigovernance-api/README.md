# ordigovernance-api

Zero-dependency contract layer: protocols, pure data shapes and the
replay policy vocabulary. This is the stable anchor that the open
runtime (`ordigovernance-runtime`), bridges and engine implementations all
build against — nothing here executes governance, everything here
defines its shape.