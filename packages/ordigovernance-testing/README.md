# ordigovernance-testing

Golden trace normalization (`normalize_bundle`, `diff_summary`), the
conformance kit (format-compatibility assertions shared by the free
tier and engine tiers), and deterministic mocks (`ScriptedLLMClient`,
mock tool handlers) for zero-infrastructure verification.